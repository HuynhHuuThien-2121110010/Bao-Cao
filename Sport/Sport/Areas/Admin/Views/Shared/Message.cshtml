@if (TempData.ContainsKey("message"))
{
    XMessenger message = (XMessenger) TempData["message"];
    <div class="alert alert-@message.TypeMsg" id="successMessage">
        @message.Msg
    </div>
}
